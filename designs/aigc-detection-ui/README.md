# 查询 AIGC 率 UI 设计稿

此目录为独立设计稿交付，不接入当前业务路由，供后续实现时参考。

## 文件

- `index.html`
  - 高保真静态预览稿，包含任务列表、上传抽屉、任务详情、移动端视图
- `styles.css`
  - 设计稿样式

## 预览方式

直接在浏览器打开 `index.html` 即可。

## 设计目标

- 将“查询 AIGC 率”明确设计为异步任务中心，而不是同步表单工具
- 首屏同时说明上传约束、任务状态与历史记录
- 强化已完成结果的可读性，突出 `AIGC 率`
- 对失败、处理中、复用历史结果三类关键状态做清晰区分
